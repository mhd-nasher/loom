# Mode C — Map User Flows

> The fast pass for "اعطني user flow" / "map the flow for X": the flow map alone — happy spine + every
> exit — without the full dimension sweep. The smallest complete deliverable Loom makes.

## Steps

### 1. Micro-discovery

Name the one feature (F-001 still applies), detect the stack only as far as the flow needs (web back
button vs mobile interruptions), note the actors in one line (F-010, table only if multi-party).

```
[█████░░░░░░░░░░░░░░░] 25% — Step 1/4: Feature + surface named
```

### 2. Draw the spine and branch it

Entry points (F-027) → happy spine (F-020) → every decision branched, every step given its unhappy exit
(F-021), abandonment noted per step (F-022), counterparty moves for two-sided features (F-024) — in the
text-diagram convention of `reference/flow-mapping.md`.

```
[██████████░░░░░░░░░░] 50% — Step 2/4: Spine drawn, exits landed
```

### 3. Mark and guard the commit point

The ★ step where the world changes: staleness re-check (F-063) and double-submit guard (F-062) noted on
the diagram. Run the flow-completeness test from `reference/flow-mapping.md`.

```
[███████████████░░░░░] 75% — Step 3/4: Commit point guarded
```

### 4. Deliver, offer the upgrade once, stop

Deliver the map with the honesty line:

> *This is the flow map only — states, lifecycle, money rules, and criteria are the full spec (Mode A),
> which this map plugs into unchanged.*

Offer the upgrade **once, in one sentence** ("if you want, this becomes a full spec — say the word"),
then **stop** (F-162). One offer is information; a second is the scope-selling the Law bans.

```
[████████████████████] 100% — Step 4/4: Flow map delivered — closed
```
